#include <bits/stdc++.h>
using namespace std;
int main(){

    //to read from binary file use mode ios::binary read and write funxtions
    //text files take more space than binary files 
    //text files are slower than binary files because of conversion to ascii

//format flags
cout<<"hello world"<<endl;
cout<<hex<<163<<endl;
cout<<dec<<163<<endl;
cout<<oct<<163<<endl;
cout<<fixed<<34.567<<endl;
cout<<scientific<<34.567<<endl;

// left	the output is padded to the field width appending fill characters at the end.
// right	the output is padded to the field width by inserting fill characters at the beginning





return 0;
}